# Database Configuration
# ====================
# For local development, choose one of these options:
#
# Option 1: Local PostgreSQL (requires PostgreSQL installed)
#   DATABASE_URL="postgresql://postgres:password@localhost:5432/daily_flow"
#
# Option 2: Docker PostgreSQL (recommended for easy setup)
#   Run: docker run --name daily-flow-db -e POSTGRES_PASSWORD=mysecretpassword -e POSTGRES_DB=daily_flow -p 5432:5432 -d postgres
#   Then use: DATABASE_URL="postgresql://postgres:mysecretpassword@localhost:5432/daily_flow"
#
# Option 3: SQLite (lightweight, file-based - change provider in schema.prisma to "sqlite")
#   DATABASE_URL="file:./dev.db"

DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/daily_flow"

# Next.js Configuration
NEXT_PUBLIC_APP_URL="http://localhost:3000"
