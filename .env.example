# Database Configuration
# ====================
# Currently using SQLite (lightweight, file-based database)
# The database file will be created at prisma/dev.db
DATABASE_URL="file:./dev.db"

# Alternative: PostgreSQL (if you prefer)
# Uncomment below and change schema.prisma provider to "postgresql"
# DATABASE_URL="postgresql://postgres:password@localhost:5432/daily_flow"

# Next.js Configuration
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Security - Encryption Key for API Keys
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY="your-32-byte-hex-encryption-key-here"
